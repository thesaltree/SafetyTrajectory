# Core LLM client
openai>=1.40.0

# Retry/backoff for transient API failures
tenacity>=8.5.0

# Exact token counting (replaces word-count heuristic)
tiktoken>=0.7.0

# Data loading & manipulation
pandas>=2.2.0
matplotlib>=3.9.0
seaborn>=0.13.0

# Schema validation
pydantic>=2.7.0

# HTTP requests (used by pandas read_csv for remote URLs)
requests>=2.32.0

# Dashboard & visualization
streamlit>=1.36.0
plotly>=5.22.0

# Async support (stdlib asyncio is sufficient; uvloop is optional speedup)
# uvloop>=0.19.0  # uncomment for Linux/macOS perf boost

# Optional: environment variable management
python-dotenv>=1.0.0
